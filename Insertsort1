#include<stdio.h>
void InsertSort1(int s[], int n)
{
	int i, j, k;
	for(i=1; i<n; i++)
	{
		for(j=i-1;j>=0;j--)
			if(s[j]<s[i])
				break;
			if(j!=i-1)
			{
				int temp = s[i];
				for(k=i-1;k>j;k--)
					s[k+1]=s[k];
				s[k+1]=temp;
			}
	}
}
int main()
 {
 	 int test[10]={2, 4, 5, 1, 60, 66, 38, 34, 35, 100};
 	  
 	 InsertSort1(test, 10);
 	 for(int k=0;k<10;k++)
	{
		printf("%d\n",test[k]);
	}
	return 1;
 }
